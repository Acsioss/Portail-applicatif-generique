// shared/js/portail.js — comportements JS transverses du portail.

document.addEventListener('DOMContentLoaded', function () {
    // Espace réservé aux comportements communs (notifications, etc.)
    // Chaque plugin ajoute son propre JS via manifest.assets.js, chargé après ce fichier.
});
